import StationsList from './_components/stations-list';
const StationStationPage = async () => {
  return (
    <main className="h-full w-full">
      <StationsList />
    </main>
  );
};

export default StationStationPage;
